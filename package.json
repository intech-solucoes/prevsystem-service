{
	"name": "@intechprev/prevsystem-service",
	"version": "2.0.0",
	"main": "dist/index.js",
	"typings": "./types/index.d.ts",
	"dependencies": {
		"@intechprev/service": "file:../_lib/service"
	},
	"devDependencies": {
		"@types/node": "10.12.18",
		"rimraf": "^2.6.3",
		"typescript": "3.2.4"
	},
	"scripts": {
		"build": "yarn && yarn clean && tsc -d --declarationDir ./types",
        "clean": "rmdir /s /q dist && rmdir /s /q types",
        "npm:publish": "yarn build && npm publish --access public"
	}
}